<UserControl x:Class="PlanningProgramV3.Views.PlanDisplayerControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PlanningProgramV3.Views"
             mc:Ignorable="d" 
             xmlns:Plan_Controls="clr-namespace:PlanningProgramV3.Views.PlanControls"
             xmlns:Converters="clr-namespace:PlanningProgramV3.Converters"
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <Converters:PixelToCoordinate x:Key="ConvertToPixel" x:Name="PixelCoordConverter"/>
    </UserControl.Resources>
    <ListView Style="{DynamicResource CustomListView}" x:Name="PlannerDisplayer" DockPanel.Dock="Top" 
        VerticalAlignment="Stretch" Background="#FFDCDCB2" AllowDrop="True" 
        HorizontalAlignment ="Stretch" 
        ItemsSource="{Binding CurrentPlan.HighestTasks}"
        ItemTemplateSelector="{StaticResource ItemTemplateSelector}"
        MouseMove="PlannerDisplayer_MouseMove" Drop="PlannerDisplayer_Drop" DragOver="PlannerDisplayer_DragOver"
        DragLeave="PlannerDisplayer_DragLeave" SelectionChanged="PlannerDisplayer_SelectionChanged">
        <!---->
        <!--Don't need item template selector for this control-->
        <ListView.ItemsPanel>
            <ItemsPanelTemplate>
                <Canvas IsItemsHost="True" Height="{Binding ElementName=PlannerDisplayer, Path=Height}"
        Width="{Binding ElementName=PlannerDisplayer, Path=Width}" />
            </ItemsPanelTemplate>
        </ListView.ItemsPanel>
        <ListView.ItemContainerStyle>
            <Style TargetType="ListViewItem">
                <Setter Property="Canvas.Left" Value="{Binding X, Converter={StaticResource ConvertToPixel}, ConverterParameter=1500, Mode=OneWay}"/>
                <Setter Property="Canvas.Top" Value="{Binding Y, Converter={StaticResource ConvertToPixel}, ConverterParameter=1500, Mode=OneWay}"/>
            </Style>
        </ListView.ItemContainerStyle>
        <ListView.ContextMenu>
            <ContextMenu x:Name="cm_AddSubItem">
                <MenuItem Header="Cut"/>
                <MenuItem Header="Copy"/>
                <MenuItem Header="Paste"/>
                <MenuItem Header="Add Task" Command="{Binding Path=CurrentPlan.AddTask}"/>
            </ContextMenu>
        </ListView.ContextMenu>
    </ListView>
</UserControl>
