<UserControl x:Class="PlanningProgramV3.Views.Calendar.CalendarTaskView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PlanningProgramV3.Views.Calendar"
             mc:Ignorable="d" x:Name="calendarTaskView"
             d:DesignHeight="450" d:DesignWidth="800">
    <TextBlock x:Name="TaskTextBlock" MaxHeight="30" Width="Auto" Height="Auto" 
               Background="{Binding ElementName=calendarTaskView, Path=BackgroundColor, UpdateSourceTrigger=PropertyChanged}"
               Text="{Binding Label, UpdateSourceTrigger=PropertyChanged}"
               MouseDown="TaskTextBlock_MouseDown"/>
    <!--Is Label a thing? Or could the Binding be to any property?-->
</UserControl>
